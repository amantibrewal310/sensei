# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/
/build

# env
# `.env` matched only that exact name and `.env*.local` needed the .local
# suffix, so `.env.production` fell through both and was committable.
.env*
!.env*.example

# misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.vercel
*.tsbuildinfo
next-env.d.ts

# sdd scratch
.superpowers/
